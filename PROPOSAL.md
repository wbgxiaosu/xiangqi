# MoonBit 中国象棋规则引擎（xiangqi）项目申报书

## 基本信息

- 项目名称：xiangqi —— MoonBit 中国象棋规则引擎库
- 参赛者：wbgxiaosu
- 联系方式：1684128842@qq.com
- GitHub 仓库链接：https://github.com/wbgxiaosu/xiangqi （master 分支 12 条有效提交）
- Gitlink 仓库链接：https://gitlink.org.cn/wbgxiaosu/xiangqi （镜像仓库，与 GitHub 同步）
- 项目方向：棋类逻辑基础库 / 游戏规则引擎
- 是否为原创项目：是。代码全部自己写的，没有移植任何现成项目；开发中用到的 perft 参考节点数来自象棋引擎社区的公开数据，只用来校验走法生成是否正确。

## 项目简介

选题之前我在 mooncakes.io 上前后搜了一百多个关键词，想找一个还没人做的方向。结果发现五子棋有、国际象棋也有，唯独中国象棋——规则最复杂、也最贴近我们自己的那一个——是空的，于是就定下了它。

象棋特别适合拿来写规则引擎：蹩马腿、塞象眼、炮打隔子、九宫、将帅照面、过河兵横走，每一条都是实打实的边界条件，写起来不难，但写对不容易。这个项目想做的事很单纯：给 MoonBit 生态补一个零依赖的象棋规则库，把走法生成、将军/将死判定、中文记谱和 FEN 解析这些底层逻辑做扎实，让后面想做象棋 AI、对战平台或者复盘工具的人不用从零开始。

## 拟实现的核心功能

- 七类棋子（车马炮兵将士象加帅/将）的走法生成，马腿、象眼、炮架这些细节全部处理，并在此基础上过滤出真正合法的走法；
- 将军检测（通过马腿、炮架的反向关系推算攻击线）、将死与困毙的判定，后续补上长将禁着；
- 中文纵线记谱的双向转换：像「炮二平五」「前车进一」这样的着法要能和坐标互转，红方用中文数字、黑方用阿拉伯数字，重名棋子带前/中/后前缀；
- XiangqiFEN 格式的解析与生成；
- 一个能在终端直接看棋盘的 Unicode 渲染器和演示小程序。

目前已经有 16 个回归测试，perft 节点数逐层和公开参考值核对。

## 现有基础

核心部分已经写完并且全部测试通过：`moon check` 没有错误和警告，16 个测试全绿。最关键的是 perft 验证——开局 perft(1)=44、perft(2)=1920、perft(3)=79666，三层都和公开参考值完全一致，说明走法生成没有计数偏差；另外把开局全部 44 种着法做了中文记谱的往返转换，逐一核对无误。仓库现在有 12 条按功能拆分的提交，README 写清了功能、用法和复现步骤，另附一份提交前的自查报告。

## 计划开发或新增的内容

1. 长将/长捉禁着的判定，把规则覆盖补到比赛级别；
2. 加一层 UCCI 协议，让引擎能接现有的象棋界面和工具；
3. 写一个简单的 α-β 剪枝搜索加局面评估，内置一个能实际对弈的 AI（不强，但能下）；
4. 发布到 mooncakes.io（`wbgxiaosu/xiangqi`），别人 `moon add` 一下就能用；
5. 把 API 文档和示例补齐。

## 预期目标和技术路线

赛期结束时的目标：MoonBit 生态第一个可发布、经过 perft 验证的象棋规则库上线 mooncakes.io，测试扩充到 30 个以上，核心 API 都有文档。

技术路线方面会保持零依赖的纯 MoonBit 实现。棋盘结构目前是不可变风格，之后给走子部分加可变路径来提速；perft 会作为走法生成的长期回归基准保留在测试里；AI 先用经典的 α-β 加置换表，不追求数值强度，代码总量控制在 4000 行以内，保证可读。测试会覆盖每类棋子的边界行为、记谱往返、FEN 对称局面和禁着规则；README 提供从零复现的完整步骤，CI 用 GitHub Actions 跑 `moon check` 和 `moon test`。
